# Figure and statistics environment (see docs/VERSIONS.md).
# Requires Python 3.11.15; pip cannot install Python itself, so use
# environment.yml with conda if you need the interpreter version pinned too.
numpy==2.4.6
pandas==2.3.3
scipy==1.17.1
statsmodels==0.14.6
scikit-learn==1.9.0
matplotlib==3.11.0
